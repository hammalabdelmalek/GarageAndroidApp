<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.garage">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/vehicule"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Garage">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".activities.NotifList" />
        <activity android:name=".Views.AlertView" />
        <activity android:name=".activities.AlertList" />
        <activity android:name=".Views.ChauffeurView" />
        <activity android:name=".Views.VehiculeView" />
        <activity android:name=".Views.MissionView" />
        <activity android:name=".activities.AddAlert" />
        <activity android:name=".activities.MissionList" />
        <activity android:name=".activities.ParkingList" />
        <activity android:name=".activities.VehiculeList" />
        <activity android:name=".activities.AjouteVehicule" />
        <activity android:name=".activities.ChauffeurList" />
        <activity android:name=".activities.ChauffeurPage" />
        <activity android:name=".activities.ChauffeurConnexion" />
        <activity android:name=".activities.AjouterChauffeur" />
        <activity android:name=".activities.AjouterMission" />
        <activity android:name=".activities.AdminPage" />
        <activity android:name=".activities.Admin_connection" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Garage.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>